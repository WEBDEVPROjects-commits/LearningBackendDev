
installing packages-npm install packageName or npm i packageName
uninstalling package-npm uninstall packageName
installing particular versions- EXAMPLE: npm install express@3.6.7 

dependencies- packages and packages ki dependencies
devdependencies-used only when we are making the app but when the app is ready we do not need these.

//scripts specifies what happens after we use commands like npm start,npm test it specifies instruction for each command start,test,etc..

start and test are default commands so they are storeed in os but custom commands are nto stored in os so we need to run such commands using (npm run commandName)